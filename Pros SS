-- [[ PROS JS SS - THE FINAL OMEGA VERSION 2026 ]] --
-- Fitur: 300+ Menu, Sky/Decal/Hint Buttons, Hoangtt Vibe, Draggable Toggle, SS Simulator

local function CreateProsJS_OMEGA()
    -- [[ DESTRUCTION OF OLD GUIS ]] --
    if game.CoreGui:FindFirstChild("ProsJSSS_Omega") then
        game.CoreGui.ProsJSSS_Omega:Destroy()
    end
    if game.CoreGui:FindFirstChild("ProsSS_Toggle_Omega") then
        game.CoreGui.ProsSS_Toggle_Omega:Destroy()
    end

    -- [[ THE OMEGA SCREEN GUI ]] --
    local ScreenGui = Instance.new("ScreenGui")
    ScreenGui.Name = "ProsJSSS_Omega"
    ScreenGui.Parent = game.CoreGui
    ScreenGui.ResetOnSpawn = false

    -- UI Settings (Ungu Neon Gelap khas Hoangtt & c00lkidd)
    local MainColor = Color3.fromRGB(10, 0, 10)
    local AccentColor = Color3.fromRGB(150, 0, 200)
    local TextColor = Color3.fromRGB(230, 50, 255)

    -- [[ 1. THE DRAGGABLE TOGGLE BUTTON ]] --
    -- Pengganti kucing Hoang VN, dengan tulisan "Pros SS"
    local ToggleGui = Instance.new("ScreenGui", game.CoreGui)
    ToggleGui.Name = "ProsSS_Toggle_Omega"

    local ToggleBtn = Instance.new("ImageButton")
    ToggleBtn.Name = "ProsSS_Launcher"
    ToggleBtn.Parent = ToggleGui
    ToggleBtn.BackgroundColor3 = MainColor
    ToggleBtn.BorderSizePixel = 2
    ToggleBtn.BorderColor3 = AccentColor
    ToggleBtn.Position = UDim2.new(0, 50, 0, 100) -- Posisi awal agak ke bawah
    ToggleBtn.Size = UDim2.new(0, 70, 0, 70) -- Gede & Bulat
    ToggleBtn.Image = "rbxassetid://8408806737" -- Logo Petir c00lkidd
    ToggleBtn.ClipsDescendants = true
    
    local UICorner = Instance.new("UICorner")
    UICorner.CornerRadius = UDim.new(1, 0)
    UICorner.Parent = ToggleBtn

    local ToggleLabel = Instance.new("TextLabel")
    ToggleLabel.Parent = ToggleBtn
    ToggleLabel.BackgroundTransparency = 1
    ToggleLabel.Size = UDim2.new(1, 0, 1, 0)
    ToggleLabel.Text = "Pros SS"
    ToggleLabel.TextColor3 = Color3.fromRGB(255, 255, 255)
    ToggleLabel.TextStrokeTransparency = 0
    ToggleLabel.TextSize = 15
    ToggleLabel.Font = Enum.Font.Code
    ToggleLabel.TextYAlignment = Enum.TextYAlignment.Bottom

    -- Bikin Draggable
    ToggleBtn.Active = true
    ToggleBtn.Draggable = true

    -- [[ 2. THE MAIN OMEGA FRAME (THE BEAST) ]] --
    local MainFrame = Instance.new("Frame")
    MainFrame.Name = "Main"
    MainFrame.Parent = ScreenGui
    MainFrame.BackgroundColor3 = MainColor
    MainFrame.BorderColor3 = AccentColor
    MainFrame.BorderSizePixel = 4
    MainFrame.Position = UDim2.new(0.5, -450, 0.5, -225)
    MainFrame.Size = UDim2.new(0, 900, 0, 450)
    MainFrame.Visible = true -- Langsung muncul pas execute

    -- Header Title
    local Header = Instance.new("TextLabel", MainFrame)
    Header.Size = UDim2.new(0, 350, 0, 35)
    Header.BackgroundColor3 = MainColor
    Header.BorderSizePixel = 0
    Header.Text = " [ PROS JS SS - OMEGA SERVER DESTROYER ] "
    Header.TextColor3 = TextColor
    Header.TextSize = 19
    Header.Font = Enum.Font.Code
    Header.TextXAlignment = "Left"

    -- [[ 3. TOP ACTION BUTTONS (SKY/DECAL/HINT/SHUTDOWN/CONSOLE) ]] --
    local TopBtns = Instance.new("Frame", MainFrame)
    TopBtns.Name = "TopButtons"
    TopBtns.BackgroundColor3 = MainColor
    TopBtns.BorderSizePixel = 0
    TopBtns.Position = UDim2.new(1, -550, 0, 2)
    TopBtns.Size = UDim2.new(0, 550, 0, 30)

    local function createTopBtn(name, text, pos, width)
        local btn = Instance.new("TextButton", TopBtns)
        btn.Name = name
        btn.BackgroundColor3 = Color3.fromRGB(30, 0, 40)
        btn.BorderColor3 = AccentColor
        btn.BorderSizePixel = 1
        btn.Size = UDim2.new(0, width or 70, 0, 25)
        btn.Position = pos
        btn.Font = Enum.Font.Code
        btn.Text = text
        btn.TextColor3 = TextColor
        btn.TextSize = 13
        return btn
    end

    -- Tombol Baru & Bawaan
    local SkyBtn = createTopBtn("SkyBtn", "skybox", UDim2.new(0, 0, 0, 2), 65)
    local DecalBtn = createTopBtn("DecalBtn", "decal", UDim2.new(0, 70, 0, 2), 65)
    local HintBtnTop = createTopBtn("HintBtnTop", "hint", UDim2.new(0, 140, 0, 2), 55)
    local ShutdownBtn = createTopBtn("Shutdown", "shutdown server", UDim2.new(0, 200, 0, 2), 120)
    local ConsoleBtn = createTopBtn("Console", "console", UDim2.new(0, 325, 0, 2), 60)

    -- Close Button (X)
    local CloseBtn = Instance.new("TextButton", TopBtns)
    CloseBtn.Name = "Close"
    CloseBtn.BackgroundColor3 = MainColor
    CloseBtn.BorderSizePixel = 0
    CloseBtn.Size = UDim2.new(0, 25, 0, 25)
    CloseBtn.Position = UDim2.new(1, -30, 0, 2)
    CloseBtn.Font = Enum.Font.SourceSansBold
    CloseBtn.Text = "X"
    CloseBtn.TextColor3 = Color3.fromRGB(200, 0, 0)
    CloseBtn.TextSize = 20

    -- [[ 4. EDITOR AREA (KIRI) ]] --
    local EditorFrame = Instance.new("ScrollingFrame", MainFrame)
    EditorFrame.Position = UDim2.new(0, 10, 0, 45)
    EditorFrame.Size = UDim2.new(0, 580, 0, 340)
    EditorFrame.BackgroundColor3 = Color3.fromRGB(5, 0, 5)

    local Editor = Instance.new("TextBox", EditorFrame)
    Editor.Size = UDim2.new(1, 0, 1, 0)
    Editor.Text = "-- Pros SS OMEGA V1\nrequire(8408806737).load('ProsJS')"
    Editor.TextColor3 = Color3.fromRGB(200, 200, 200)
    Editor.Font = Enum.Font.Code
    Editor.TextSize = 14
    Editor.MultiLine = true
    Editor.TextXAlignment = "Left"
    Editor.TextYAlignment = "Top"
    Editor.ClearTextOnFocus = false

    -- [[ 5. THE 300+ FEATURES PANEL (KANAN) ]] --
    local FeaturePanel = Instance.new("ScrollingFrame", MainFrame)
    FeaturePanel.Position = UDim2.new(0, 600, 0, 45)
    FeaturePanel.Size = UDim2.new(0, 290, 0, 395)
    FeaturePanel.BackgroundColor3 = Color3.fromRGB(20, 0, 25)
    FeaturePanel.CanvasSize = UDim2.new(0, 0, 14, 0) -- Panjang banget biar muat 300+

    local UIList = Instance.new("UIListLayout", FeaturePanel)
    UIList.Padding = UDim.new(0, 5)

    -- Fungsi Tambah Fitur
    local function addFeature(name)
        local btn = Instance.new("TextButton", FeaturePanel)
        btn.BackgroundColor3 = Color3.fromRGB(40, 0, 50)
        btn.Size = UDim2.new(1, -10, 0, 30)
        btn.Text = " > " .. name
        btn.TextColor3 = TextColor
        btn.Font = Enum.Font.Code
    end

    -- Contoh Fitur Utama
    addFeature("SERVER DESTROYER V1")
    addFeature("C00LKIDD SKY (SS)")
    addFeature("SHUTDOWN SERVER")
    addFeature("KILL ALL PLAYERS (SS)")
    addFeature("SPAM C00LKIDD DECAL")
    addFeature("NUKE MAP")
    addFeature("FLING ALL (FE)")
    addFeature("GIVE ADMIN (SS-LOCKED)")

    -- Auto-generate placeholders (Total 300+)
    for i = 1, 302 do addFeature("Pros_Command_" .. i .. " [SS-LOCKED]") end

    -- [[ 6. BOTTOM BUTTONS (HOANGTT STYLE) ]] --
    local BottomBtns = Instance.new("Frame", MainFrame)
    BottomBtns.Name = "BottomButtons"
    BottomBtns.BackgroundColor3 = MainColor
    BottomBtns.BorderSizePixel = 0
    BottomBtns.Position = UDim2.new(0, 10, 0, 400)
    BottomBtns.Size = UDim2.new(0, 580, 0, 35)

    local function createBtmBtn(name, text, pos)
        local btn = Instance.new("TextButton", BottomBtns)
        btn.Name = name
        btn.BackgroundColor3 = Color3.fromRGB(25, 0, 35)
        btn.BorderColor3 = AccentColor
        btn.BorderSizePixel = 1
        btn.Size = UDim2.new(0, 110, 0, 35)
        btn.Position = pos
        btn.Font = Enum.Font.Code
        btn.Text = text
        btn.TextColor3 = TextColor
        btn.TextSize = 16
        return btn
    end

    createBtmBtn("Execute", UDim2.new(0, 0, 0, 0))
    createBtmBtn("Clear", UDim2.new(0, 120, 0, 0))
    createBtmBtn("ScanSS", UDim2.new(0, 240, 0, 0))
    createBtmBtn("FEBypass", UDim2.new(0, 360, 0, 0))
    createBtmBtn("KillAll", UDim2.new(0, 480, 0, 0))

    -- [[ LOGIC ]] --

    -- 1. Toggle Button Logic
    ToggleBtn.MouseButton1Click:Connect(function()
        MainFrame.Visible = not MainFrame.Visible
        print("Pros SS: Menu Toggled!")
    end)

    -- 2. Skybox Petir
    SkyBtn.MouseButton1Click:Connect(function()
        local s = Instance.new("Sky", game.Lighting)
        s.SkyboxBk = "rbxassetid://8408806737" s.SkyboxDn = "rbxassetid://8408806737"
        s.SkyboxFt = "rbxassetid://8408806737" s.SkyboxLf = "rbxassetid://8408806737"
        s.SkyboxRt = "rbxassetid://8408806737" s.SkyboxUp = "rbxassetid://8408806737"
    end)

    -- 3. Hint
    HintBtnTop.MouseButton1Click:Connect(function()
        local h = Instance.new("Hint", workspace)
        h.Text = "Pros SS Has Joined! Say Goodbye!"
        task.wait(4) h:Destroy()
    end)

    CloseBtn.MouseButton1Click:Connect(function() ScreenGui:Destroy() end)

    print("Pros JS SS OMEGA Version Loaded! 310 Features Ready.")
end

CreateProsJS_OMEGA()
