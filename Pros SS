-- [[ PROS JS SS - ULTIMATE CHAOS EDITION 2026 ]] --
-- Fitur: 300+ Menu (Scrollable), Auto-Scan, Server-Side Simulation

local function CreateProsJS_Ultimate()
    if game.CoreGui:FindFirstChild("ProsJSSS_Ultimate") then
        game.CoreGui.ProsJSSS_Ultimate:Destroy()
    end

    local ScreenGui = Instance.new("ScreenGui")
    local MainFrame = Instance.new("Frame")
    local Header = Instance.new("TextLabel")
    local CloseBtn = Instance.new("TextButton")
    local TopBtns = Instance.new("Frame")
    local EditorFrame = Instance.new("ScrollingFrame")
    local Editor = Instance.new("TextBox")
    
    -- Panel Fitur Baru (Sebelah Kanan)
    local FeaturePanel = Instance.new("ScrollingFrame")
    local UIList = Instance.new("UIListLayout")

    -- UI Settings (Ungu Neon Gelap)
    local MainColor = Color3.fromRGB(10, 0, 10) 
    local AccentColor = Color3.fromRGB(150, 0, 200) 
    local TextColor = Color3.fromRGB(230, 50, 255) 

    ScreenGui.Name = "ProsJSSS_Ultimate"
    ScreenGui.Parent = game.CoreGui

    -- Frame Utama (Dibikin makin lebar buat nampung menu 300+)
    MainFrame.Name = "Main"
    MainFrame.Parent = ScreenGui
    MainFrame.BackgroundColor3 = MainColor
    MainFrame.BorderColor3 = AccentColor
    MainFrame.BorderSizePixel = 4
    MainFrame.Position = UDim2.new(0.5, -450, 0.5, -225)
    MainFrame.Size = UDim2.new(0, 900, 0, 450)
    MainFrame.Active = true
    MainFrame.Draggable = true

    -- Header Title
    Header.Parent = MainFrame
    Header.BackgroundColor3 = MainColor
    Header.Size = UDim2.new(0, 400, 0, 35)
    Header.Font = Enum.Font.Code
    Header.Text = " [ PROS JS SS - THE SERVER DESTROYER ] "
    Header.TextColor3 = TextColor
    Header.TextSize = 20
    Header.TextXAlignment = Enum.TextXAlignment.Left

    -- Top Button Bar (Sky, Decal, Hint, Shutdown, Console)
    TopBtns.Parent = MainFrame
    TopBtns.BackgroundColor3 = MainColor
    TopBtns.Position = UDim2.new(1, -500, 0, 5)
    TopBtns.Size = UDim2.new(0, 500, 0, 30)

    local function createTopBtn(text, pos, width, func)
        local btn = Instance.new("TextButton", TopBtns)
        btn.BackgroundColor3 = Color3.fromRGB(30, 0, 40)
        btn.BorderColor3 = AccentColor
        btn.Size = UDim2.new(0, width, 0, 25)
        btn.Position = pos
        btn.Font = Enum.Font.Code
        btn.Text = text
        btn.TextColor3 = TextColor
        btn.TextSize = 13
        btn.MouseButton1Click:Connect(func)
        return btn
    end

    -- Mini Actions
    createTopBtn("skybox", UDim2.new(0, 0, 0, 0), 65, function()
        local s = Instance.new("Sky", game.Lighting)
        s.SkyboxBk = "rbxassetid://8408806737" s.SkyboxDn = "rbxassetid://8408806737"
        s.SkyboxFt = "rbxassetid://8408806737" s.SkyboxLf = "rbxassetid://8408806737"
        s.SkyboxRt = "rbxassetid://8408806737" s.SkyboxUp = "rbxassetid://8408806737"
    end)
    createTopBtn("shutdown", UDim2.new(0, 70, 0, 0), 100, function() print("Attempting Shutdown...") end)
    createTopBtn("hint", UDim2.new(0, 175, 0, 0), 50, function()
        local h = Instance.new("Hint", workspace)
        h.Text = "Pros SS Has Joined! Say Goodbye!"
        task.wait(4) h:Destroy()
    end)

    -- Editor Area (Kiri)
    EditorFrame.Parent = MainFrame
    EditorFrame.BackgroundColor3 = Color3.fromRGB(5, 0, 5)
    EditorFrame.Position = UDim2.new(0, 10, 0, 45)
    EditorFrame.Size = UDim2.new(0, 580, 0, 340)

    Editor.Parent = EditorFrame
    Editor.Size = UDim2.new(1, 0, 1, 0)
    Editor.BackgroundColor3 = Color3.fromRGB(5, 0, 5)
    Editor.Font = Enum.Font.Code
    Editor.Text = "-- PROS JS SS READY\nrequire(8408806737).load('ProsJS')\n-- Paste your malicious code here..."
    Editor.TextColor3 = Color3.fromRGB(180, 180, 180)
    Editor.TextSize = 14
    Editor.MultiLine = true
    Editor.TextXAlignment = "Left"
    Editor.TextYAlignment = "Top"

    -- PANEL 300+ FITUR (Kanan)
    FeaturePanel.Parent = MainFrame
    FeaturePanel.BackgroundColor3 = Color3.fromRGB(20, 0, 25)
    FeaturePanel.BorderColor3 = AccentColor
    FeaturePanel.Position = UDim2.new(0, 600, 0, 45)
    FeaturePanel.Size = UDim2.new(0, 290, 0, 395)
    FeaturePanel.CanvasSize = UDim2.new(0, 0, 10, 0) -- Panjang banget biar muat 300+

    UIList.Parent = FeaturePanel
    UIList.Padding = BoxExtend.new(0, 5)
    UIList.SortOrder = Enum.SortOrder.LayoutOrder

    local function addFeature(name, func)
        local btn = Instance.new("TextButton", FeaturePanel)
        btn.BackgroundColor3 = Color3.fromRGB(40, 0, 50)
        btn.Size = UDim2.new(1, -10, 0, 30)
        btn.Font = Enum.Font.Code
        btn.Text = " > " .. name
        btn.TextColor3 = TextColor
        btn.TextSize = 14
        btn.MouseButton1Click:Connect(func)
    end

    -- ISI 300+ FITUR (Contoh Utama, Sisanya Auto-Generated)
    addFeature("KILL ALL PLAYERS (SS)", function() warn("Scanning for Kill Remote...") end)
    addFeature("SERVER CRASHER V3", function() warn("Sending Lag Packets...") end)
    addFeature("SPAM C00LKIDD DECAL", function() warn("Injecting Decals...") end)
    addFeature("BAN ALL (ADMIN ONLY)", function() warn("Access Denied: Need SS Backdoor") end)
    addFeature("RAINBOW EVERYTHING", function() warn("Applying Rainbow Shader...") end)
    addFeature("GIVE ALL BADGES", function() warn("Badge Remote not found.") end)
    addFeature("NUKE MAP", function() warn("Destroying Workspace...") end)
    addFeature("KIDNAP PLAYER (FE)", function() warn("Select Player to Fling!") end)

    -- Auto-generate placeholders biar keliatan OP parah (Total 300+)
    for i = 1, 292 do
        addFeature("Command_0" .. i .. " [SS-LOCKED]", function() warn("Backdoor Needed for this command!") end)
    end

    -- Bottom Buttons
    local function createBtmBtn(text, pos)
        local btn = Instance.new("TextButton", MainFrame)
        btn.BackgroundColor3 = Color3.fromRGB(25, 0, 35)
        btn.BorderColor3 = AccentColor
        btn.Size = UDim2.new(0, 110, 0, 35)
        btn.Position = pos
        btn.Font = Enum.Font.Code
        btn.Text = text
        btn.TextColor3 = TextColor
        btn.TextSize = 15
        return btn
    end

    createBtmBtn("Execute", UDim2.new(0, 10, 0, 400))
    createBtmBtn("Clear", UDim2.new(0, 130, 0, 400))
    createBtmBtn("Scan SS", UDim2.new(0, 250, 0, 400))
    createBtmBtn("FE Bypass", UDim2.new(0, 370, 0, 400))
    createBtmBtn("Kill all", UDim2.new(0, 490, 0, 400))

    -- Close Logic
    CloseBtn.Parent = MainFrame
    CloseBtn.Position = UDim2.new(1, -30, 0, 5)
    CloseBtn.Size = UDim2.new(0, 25, 0, 25)
    CloseBtn.Text = "X"
    CloseBtn.TextColor3 = Color3.fromRGB(255, 0, 0)
    CloseBtn.MouseButton1Click:Connect(function() ScreenGui:Destroy() end)

    print("PROS JS SS: ULTIMATE CHAOS EDITION LOADED! 310 Features Detected.")
end

CreateProsJS_Ultimate()
